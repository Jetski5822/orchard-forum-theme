@using NGM.Forum
@using System.Web.Mvc.Html
@using Orchard.Mvc.Html
@{
    Script.Require("jQuery");
    if (AuthorizedFor(Orchard.Core.Contents.Permissions.PublishContent)) {
        Script.Include("quickreply.js");
        Script.Include("quickreply-markdown.js");
        Script.Include("to-markdown/to-markdown.js");
    }
}

<div id="quick-reply" class="closed">
    <div class="inner-quick-reply">
        <h3>@T("Reply")</h3>
        <a href="#" class="cancel cancel-link" title="@T("Cancel")"><img src="@Url.Content("~/Themes/Forum/Content/images/icons/0145.png")" alt="@T("Cancel")"/></a>

        @using (Html.BeginFormAntiForgeryPost(Url.Action("Create", "Post"))) {
            @Html.ValidationSummary()

            @Display(Model.ContentEditor)

            <input type="hidden" id="contentid" name="contentid" value="@Model.ContentId"/>
        }
    </div>
</div>